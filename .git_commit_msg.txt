refactor(GC-023/barrel-split): extract CPF consumer pipeline bridges to domain barrel

Problem: CPF index.ts grew to 1108 lines, exceeding the 1000-line hard limit.
Wrong fix: exception cap was bumped 1100->1200 instead of splitting the file.
Correct fix: barrel split applied per requiredFollowup in the exception registry.

Action:
- Extracted all consumer pipeline bridge re-exports (W1-T13 through W2-T35, ~450 lines)
  into consumer.pipeline.bridges.barrel.ts
- Replaced them in index.ts with: export * from "./consumer.pipeline.bridges.barrel"
- index.ts: 1108 -> 659 lines (under 1000 hard limit, no exception needed)
- consumer.pipeline.bridges.barrel.ts: ~454 lines (under 700 advisory)
- Removed CPF index.ts ACTIVE_EXCEPTION from CVF_GOVERNED_FILE_SIZE_EXCEPTION_REGISTRY.json

All 1741 CPF tests pass (1 pre-existing failure in gateway.consumer.test.ts is
unrelated to this refactor, confirmed via git log on that file).

Size guard: COMPLIANT (no exception)
