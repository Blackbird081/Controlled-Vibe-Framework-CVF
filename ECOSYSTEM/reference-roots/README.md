# ECOSYSTEM Reference Roots

Internal retained subtree for low-footprint roots that no longer belong in the visible repository root.

Current contents:

- `CVF_SKILL_LIBRARY/`
- `ui_governance_engine/`

Rules:

- these paths are retained for lineage and internal reference only
- they are not public-export signals
- future additions still require a bounded `GC-019` packet and must satisfy `GC-039`
