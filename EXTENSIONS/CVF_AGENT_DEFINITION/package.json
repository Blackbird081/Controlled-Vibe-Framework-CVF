{
  "name": "cvf-agent-definition",
  "version": "0.1.0",
  "description": "CVF Agent Definition — Coordination package unifying agent identity runtime and capability governance baseline",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "private": true,
  "scripts": {
    "test": "vitest run --config vitest.config.ts",
    "test:coverage": "vitest run --coverage --config vitest.config.ts",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "keywords": ["cvf", "agent-definition", "identity", "capability"],
  "devDependencies": {
    "@vitest/coverage-v8": "^1.6.1",
    "typescript": "^5.4.0",
    "vitest": "^1.6.1"
  }
}
