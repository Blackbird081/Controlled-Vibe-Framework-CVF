{
  "name": "cvf-agent-ledger",
  "version": "0.1.0",
  "description": "CVF Agent Ledger — Merged learning-plane package for task history and reputation runtime",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "private": true,
  "scripts": {
    "test": "vitest run --config vitest.config.ts",
    "test:coverage": "vitest run --coverage --config vitest.config.ts",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "keywords": ["cvf", "agent-ledger", "task-marketplace", "reputation"],
  "devDependencies": {
    "@vitest/coverage-v8": "^1.6.1",
    "typescript": "^5.4.0",
    "vitest": "^1.6.1"
  }
}
