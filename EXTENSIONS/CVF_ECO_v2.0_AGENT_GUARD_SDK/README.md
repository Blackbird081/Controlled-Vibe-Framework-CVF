# CVF ECO v2.0 Agent Guard SDK

This source module now participates in the approved `B* Merge 4` umbrella:

- canonical coordination package: `EXTENSIONS/CVF_TRUST_SANDBOX/`
- execution class: `coordination package`

Current-cycle note:

- this SDK remains the lightweight embedded trust surface
- `CVF_v1.7.1_SAFETY_RUNTIME` remains the full runtime line
- no physical merge has been approved in the current cycle
