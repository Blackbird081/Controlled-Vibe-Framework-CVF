# CVF ECO v2.3 — Agent Identity

> This module is now part of the coordination package [`CVF_AGENT_DEFINITION`](../CVF_AGENT_DEFINITION/).

Current role:

- runtime identity, credential, trust, and permission management
- preserved as the canonical runtime source for `agent definition`

Related source:

- capability governance baseline:
  - [`CVF_v1.2_CAPABILITY_EXTENSION`](../CVF_v1.2_CAPABILITY_EXTENSION/)

Execution posture:

- approved under `GC-019` as a `coordination package`
- source lineage preserved
