{
  "name": "cvf-learning-plane-foundation",
  "version": "0.1.0",
  "description": "CVF Learning Plane Foundation - first usable learning-plane slice for feedback ledger and pattern detection",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "private": true,
  "scripts": {
    "test": "vitest run --config vitest.config.ts",
    "test:coverage": "vitest run --coverage --config vitest.config.ts",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "keywords": ["cvf", "learning-plane", "foundation", "pattern-detection", "feedback-ledger"],
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@vitest/coverage-v8": "^1.6.1",
    "typescript": "^5.4.0",
    "vitest": "^1.6.1"
  }
}
