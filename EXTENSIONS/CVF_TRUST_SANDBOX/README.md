# CVF Trust Sandbox

`CVF_TRUST_SANDBOX` is the approved `B* Merge 4` coordination package.

Current-cycle execution class:

- `coordination package`
- `CVF_v1.7.1_SAFETY_RUNTIME` remains the full runtime line
- `CVF_ECO_v2.0_AGENT_GUARD_SDK` remains the lightweight embeddable SDK
- no physical merge occurs in this cycle

Use this package to understand the trust-domain split:

- choose `CVF_v1.7.1_SAFETY_RUNTIME` for full policy/runtime/simulation flows
- choose `CVF_ECO_v2.0_AGENT_GUARD_SDK` for lightweight local guard/risk evaluation
