{
    "name": "cvf-safety-hardening",
    "version": "1.8.0",
    "description": "CVF v1.8 Safety Hardening — Deterministic AI Execution Kernel",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "tsc --noEmit",
        "test": "vitest run",
        "test:watch": "vitest",
        "test:coverage": "vitest run --coverage",
        "lint": "eslint . --ext .ts",
        "check": "npm run build && npm run test"
    },
    "devDependencies": {
        "@types/node": "^22.0.0",
        "@typescript-eslint/eslint-plugin": "^8.0.0",
        "@typescript-eslint/parser": "^8.0.0",
        "@vitest/coverage-v8": "^3.2.4",
        "eslint": "^9.0.0",
        "typescript": "^5.7.0",
        "vitest": "^3.0.0"
    }
}