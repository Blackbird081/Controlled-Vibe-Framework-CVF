{
    "name": "cvf-deterministic-reproducibility",
    "version": "1.9.0",
    "description": "CVF v1.9 Deterministic Reproducibility — Replay, forensic audit, context freeze",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "tsc --noEmit",
        "test": "vitest run",
        "test:coverage": "vitest run --coverage",
        "check": "npm run build && npm run test"
    },
    "devDependencies": {
        "@types/node": "^22.0.0",
        "@vitest/coverage-v8": "^3.2.4",
        "typescript": "^5.7.0",
        "vitest": "^3.0.0"
    }
}