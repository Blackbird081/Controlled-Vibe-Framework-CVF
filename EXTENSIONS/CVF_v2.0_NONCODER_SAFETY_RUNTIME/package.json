{
    "name": "cvf-noncoder-safety-runtime",
    "version": "2.0.0",
    "description": "CVF v2.0 Non-Coder Safety Runtime — Mode abstraction and intent interpreter",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "tsc --noEmit",
        "test": "vitest run",
        "test:coverage": "vitest run --coverage",
        "check": "npm run build && npm run test"
    },
    "devDependencies": {
        "@types/node": "^22.0.0",
        "@vitest/coverage-v8": "^3.2.4",
        "typescript": "^5.7.0",
        "vitest": "^3.0.0"
    }
}